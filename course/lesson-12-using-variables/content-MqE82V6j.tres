[gd_resource type="Resource" load_steps=2 format=2]

[ext_resource path="res://resources/ContentBlock.gd" type="Script" id=1]

[resource]
script = ExtResource( 1 )
text = "The [code]set_angular_speed()[/code] function can't use the [code]angular_speed[/code] variable because we defined it in another function.

Here's what this error might look like."
visual_element_path = ""
reverse_blocks = false
type = 0
