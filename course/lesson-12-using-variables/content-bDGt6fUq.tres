[gd_resource type="Resource" load_steps=2 format=2]

[ext_resource path="res://resources/ContentBlock.gd" type="Script" id=1]

[resource]
script = ExtResource( 1 )
title = ""
type = 0
text = "We can also define variables inside of functions.

We align the variable assignment using tab to make it part of the function body. Make sure to create the variable before using it!

If we define a variable inside of a function, only that function can use it."
visual_element_path = ""
reverse_blocks = false
has_separator = false
