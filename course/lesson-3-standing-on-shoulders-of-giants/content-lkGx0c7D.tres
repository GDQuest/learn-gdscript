[gd_resource type="Resource" load_steps=2 format=2]

[ext_resource path="res://resources/ContentBlock.gd" type="Script" id=1]

[resource]
script = ExtResource( 1 )
text = "With the [code]move_local_x()[/code] function, you can move the character to its left and right.

[i]The example below is a more complicated than we'd like. Please don't worry about the new lines and keywords; it's just so the character oscillates and doesn't go outside the view.[/i]"
visual_element_path = ""
reverse_blocks = false
type = 0
