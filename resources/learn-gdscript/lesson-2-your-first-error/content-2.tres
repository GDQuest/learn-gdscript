[gd_resource type="Resource" load_steps=2 format=2]

[ext_resource path="res://resources/ContentBlock.gd" type="Script" id=1]

[resource]
script = ExtResource( 1 )
text = "In this lesson you'll face your first error.

At first, errors can be a little cryptic and hard to read.

But fear not! Learn GDScript comes with a built-in error translator. It gives you:

1. The actual message you'll get when making an error with GDScript.
2. An explanation of what the message means and why the error happens.
3. Ways you can go about solving the error and making your code run."
visual_element_path = "path/to/error-demo.tscn"
reverse_blocks = false
